/**
 * Responsive renderer style constants
 */
export const responsiveStyles = {
  container: 'relative w-full',
  hiddenItem: 'invisible absolute top-0 left-0 w-0 h-0 overflow-hidden',
  visibleItem: 'visible relative w-full',
  loadingContainer: 'flex items-center justify-center min-h-[20px] w-full',
};
